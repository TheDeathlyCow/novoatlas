- Add a new density function type: `novoatlas:get_preliminary_height_from_map`
- This is essentially the same as `novoatlas:get_height_from_map`, but subtracts the elevation value by 8 to account for how the generator expects the preliminary surface value to be slightly lower than the final value.
- This also fixes a long standing issue with aquifer barriers generating near shoreline excessively.